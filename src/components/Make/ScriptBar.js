import React from 'react';
import { Navbar, Nav } from '../MyBootstrap';

const ScriptBar = ({distro, options, name}) => {
  return (
        <Nav>sudo crouton ..</Nav>
  );
}

export default ScriptBar;